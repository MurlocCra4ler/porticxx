#pragma once

#include <type_traits>

namespace std {

namespace imp_concepts {
    template< class T, class U >
    concept same_helper = std::is_same_v<T, U>;
}
 
template< class T, class U >
concept same_as = imp_concepts::same_helper<T, U> && imp_concepts::same_helper<U, T>;

}