#pragma once

#include <cstddef>
//#include <bits/memory/allocators.hpp>

void* operator new(std::size_t size);

[[nodiscard]] inline constexpr void* operator new (std::size_t count, void* ptr) { return ptr; }